<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fedunimillionaire30350727">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FedUniMillionaire30350727">
        <activity
            android:name=".GameStatusAndLifeLineActivity"
            android:label="@string/title_activity_game_status_and_life_line"
            android:theme="@style/Theme.FedUniMillionaire30350727.NoActionBar"></activity>
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>